name: Simple Shell Command

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"

      - name: multilien script
        run: |
          node -v
          npm -v
      - name: Directory
        run: pwd